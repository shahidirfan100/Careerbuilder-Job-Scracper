{
  "title": "CareerBuilder Jobs Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "string",
      "description": "Direct URL to scrape (optional - leave empty to use keyword search)",
      "editor": "textarea",
      "default": ""
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job titles or keywords (e.g., 'Software Engineer')",
      "editor": "textfield",
      "prefill": "Software Engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City, state, or zip code (e.g., 'New York, NY')",
      "editor": "textfield"
    },
    "results_wanted": {
      "title": "Number of Jobs",
      "type": "integer",
      "description": "Maximum number of jobs to scrape",
      "editor": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 500
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum listing pages to process",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    },
    "cookiesJson": {
      "title": "Cookies (JSON)",
      "type": "string",
      "description": "Optional: Paste browser cookies in JSON format to bypass blocking. Format: [{\"name\":\"cookie_name\",\"value\":\"cookie_value\"}] or {\"name\":\"value\"}",
      "editor": "textarea",
      "example": "[{\"name\":\"sessionid\",\"value\":\"abc123\",\"domain\":\".careerbuilder.com\"}]"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "REQUIRED: USA Residential proxy to bypass geo-blocking",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ],
        "countryCode": "US"
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ],
        "countryCode": "US"
      }
    }
  }
}