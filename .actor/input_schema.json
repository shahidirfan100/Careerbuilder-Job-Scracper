{
  "title": "CareerBuilder Job Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "keyword": {
      "title": "Job Keywords",
      "type": "string",
      "description": "Search for specific job titles or keywords (e.g., 'Software Engineer', 'Marketing Manager')",
      "editor": "textfield",
      "example": "Software Engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City, state, or zip code (e.g., 'New York, NY' or '10001')",
      "editor": "textfield",
      "example": "New York, NY"
    },
    "posted_date": {
      "title": "Posted Date",
      "type": "string",
      "description": "Filter jobs by posting date",
      "editor": "select",
      "default": "anytime",
      "enum": ["anytime", "24h", "7d", "30d"],
      "enumTitles": ["Anytime", "Last 24 hours", "Last 7 days", "Last 30 days"]
    },
    "results_wanted": {
      "title": "Number of Jobs",
      "type": "integer",
      "description": "Maximum number of jobs to scrape",
      "editor": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 10000,
      "unit": "jobs"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of listing pages to process",
      "editor": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 100,
      "unit": "pages"
    },
    "startUrl": {
      "title": "Custom Start URL (Optional)",
      "type": "string",
      "description": "Provide a direct CareerBuilder search URL. This will override keyword/location settings.",
      "editor": "textfield",
      "pattern": "^https://www\\.careerbuilder\\.com/.*",
      "example": "https://www.careerbuilder.com/jobs?keywords=developer&location=remote"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Use Apify Proxy to avoid being blocked (highly recommended)",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      },
      "prefill": {
        "useApifyProxy": true
      }
    }
  },
  "required": []
}